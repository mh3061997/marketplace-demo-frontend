import { LimitedQuantityPipePipe } from './limited-quantity-pipe.pipe';

describe('LimitedQuantityPipePipe', () => {
  it('create an instance', () => {
    const pipe = new LimitedQuantityPipePipe();
    expect(pipe).toBeTruthy();
  });
});
